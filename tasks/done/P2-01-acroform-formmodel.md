# P2-01 — AcroForm FormModel: Read & Write (Freeze Point)

**Epic:** E7 · **Primary package:** `Packages/DocEngineHost` (forms) + `Packages/PDFEngineAPI` (`FormField` extension, ADR-016) `[INTEGRATION]` · **Complexity:** L · **Priority:** Critical

## Goal
Implement the PDFEngineAPI `FormModel`: parse AcroForm field trees (name, kind, rect, format hints, tooltip, tab order, current values) and write values back — the substrate for fill UI and autofill.

## Background
PRD FR-1.8; ARCHITECTURE.md freeze table: FormModel v1 unblocks P2-02/03/05 and FormKnowledge. Field-tree fidelity here bounds everything autofill can do — invest in the weird cases (inherited attributes, comb fields, radio groups with same names, JS format strings parsed as *hints* only).

## Requirements
- Read: full field tree incl. widget annotations, export values for checkboxes/radios, choice options, comb/maxlen, read-only/required flags, format/validation hints (safe subset — no JS execution).
- Write: set values with appearance-stream regeneration so filled forms render everywhere (NFR-C2); needAppearances handling.
- Field-change notifications to consumers; document-modified integration with save path.

## Dependencies
- P1-16.

## Files Likely Affected
- `Packages/DocEngineHost/Sources/Forms/**`.

## Acceptance Criteria
- Fixture forms (W-9, I-9, DS-160-class, medical intake set) parse to trees matching expected-field manifests; values written by us render correctly in Acrobat and Preview.
- No JS execution pathway exists (security review checkbox).

## Definition of Done
- Global DoD, plus: FormModel v1 freeze noted in ADR-016 (this task file originally cited ADR-010, which is already taken by `docs/adr/ADR-010-inferenceapi-v1.md` — a stale reference, corrected here).

## Testing Requirements
- Tree-parity tests vs manifests; write→reopen→read equivalence; appearance snapshot tests; interop fixtures.

## Documentation Updates
- `DocEngineHost/CLAUDE.md` forms section; expected-field manifest format doc.

## Journal

**Orient/Plan (frozen-seam blocker, resolved):** `PDFEngineAPI.FormField` (frozen, ADR-006) as it stood couldn't carry export values, radio-group identity, choice options, or a required flag — all explicitly named in this task's own Requirements. Per CLAUDE.md §3.6/§3.7, a frozen-seam shape change needs an ADR before code, not a workaround. Wrote `docs/adr/ADR-016-pdfengineapi-formfield-export-values.md`: adds `exportValue`, `groupName`, `choiceOptions`, `isRequired` to `FormField`, all defaulted so the only existing call sites (`FakePDFEngine` + its tests, both in `PDFEngineAPI` itself) keep compiling unmodified — confirmed via `Scripts/verify.sh PDFEngineAPI` (OK). Per ADR-008 this is self-mergeable once the ADR is present and CI is green, so proceeding rather than escalating for human sign-off. Task reclassified `[INTEGRATION]` (now spans `PDFEngineAPI` + `DocEngineHost`) — also corrected this file's DoD line, which cited ADR-010 (already taken by `docs/adr/ADR-010-inferenceapi-v1.md`, unrelated); used ADR-016, the actual next-free number.

**Plan (DocEngineHost half):** `fpdf_formfill.h`/`fpdf_annot.h` are already vendored and in `CPDFium`'s modulemap — no new header needed. Field introspection/write requires an `FPDF_FORMHANDLE` from `FPDFDOC_InitFormFillEnvironment`, which needs a fully-populated `FPDF_FORMFILLINFO` callback struct (PDFium calls several fields unconditionally even for non-interactive use) — wrote `Forms/FormFillEnvironment.swift`, a no-op implementation of all ~29 callbacks (real signatures verified against the vendored header directly, several diverged from the doc-comment line count — e.g. `FFI_DoGoToAction` is 5 params not 6, `FFI_PopupMenu` takes an `FPDF_WIDGET` making it 6 not 8 — confirmed by bisecting `swift build` failures since Swift 6's diagnostic for a wrong `@convention(c)` closure signature inside a huge struct-literal initializer is unhelpful ("failed to produce diagnostic")). Plan for `Forms/PDFiumFormModel.swift`: `PDFiumEngine: FormModel` conformance — `fields(of:)` walks `FPDFPage_GetAnnotCount`/`GetAnnot` per page filtering to `FPDF_ANNOT_WIDGET` subtype, reads name/alt-name(tooltip)/type/value/flags/export-value/options via the `FPDFAnnot_GetFormField*`/`GetOption*` family; `setValue` finds the widget by name and calls the corresponding `SetFormField*`-family setter, following existing `PDFiumAnnotationStore` conventions (UTF-16LE buffer-length-then-fill pattern, typed `PDFEngineError`). Comb/maxlen already fit in `FormatHint` (no gap). Radio-group `groupName`: PDFium enumerates each radio widget as a distinct field by its own fully-qualified name when siblings differ only by `/AS`; grouping widgets that share a `/Parent` field name via `FPDFAnnot_GetFormControlIndex`/`Count`. Format-hint JS strings (if any `/AA` action exists) are read as inert strings only — no JS engine is even compiled into this vendored PDFium build (`pdf_enable_v8=false`), so evaluation is structurally impossible, not just avoided by convention. Fixtures: no real W-9/I-9/DS-160/medical-intake PDFs exist in `Fixtures/` yet (checked) — building synthetic AcroForm PDFs (via PDFium's own edit API, matching the existing annotation-fixture-building pattern) that exercise text/comb/checkbox/radio-group/choice-field/required-flag is the pragmatic scope, not a byte-for-byte real-form replica; documented as a scope interpretation, not silently narrowed.

**Correction (Implement):** the fixture claim above was wrong — `Fixtures/forms/manifest.json` (P0-08) already has 5 real government AcroForms including W-9 and I-9 (the exact two the task names), each with an expected-field-name list, doubling as `pdf-corpus/starter/`'s files. Used these directly instead of synthetic PDFs: no new fixtures needed, and real forms are strictly better test material than hand-built ones.

**Implement:** `Forms/FormFillEnvironment.swift` (no-op `FPDF_FORMFILLINFO`, ~1hr of signature bisection against real Swift 6 compiler errors, see above) → `PDFiumEngine.OpenDocument.formHandle` (lazy, torn down in `close`) → `Forms/PDFiumFormModel.swift` (`fields(of:)`/`setValue`, full doc comment covers the identity/tab-order/write-path decisions) → `PDFiumFormModelTests.swift`, data-driven off `Fixtures/forms/manifest.json`: tree-parity (every manifest `field_name` present, comparing against `groupName ?? name`), field-id uniqueness across all 5 real forms, a real write→`save()`→reopen→read round trip against a scratch copy of the W-9 (never mutates the checked-in fixture), and a `.fieldNotFound` negative test. Also added `testFormModelConformanceAgainstRealFixture` invoking the frozen `PDFEngineConformanceSuite.verifyFormModel` (pre-existing, generic over any `FormModel` — no suite changes needed) against the real engine, matching `PDFiumAnnotationStoreTests`'s existing pattern. One test (`testRadioGroupSiblingsGetDistinctIDsSharingOneGroupName`) had to be redesigned mid-implementation: assumed the W-9's 7-widget "Line 3a" classification group would collide field names (triggering the `#<index>` disambiguation) — it doesn't; PDFium already returns a unique name per widget for every real multi-widget group in this whole corpus. Replaced with a general "every field id is unique, on every fixture" invariant instead of a fabricated collision scenario; documented the real gap (disambiguation logic implemented + unit-correct, but never exercised by a real collision) in `DocEngineHost/CLAUDE.md`.

**Verify:** `Scripts/verify.sh DocEngineHost` — OK (real `swift build` + `swift test` — full Xcode.app available, ran for real — + boundary lint). All 6 new/changed tests pass.

**Harden notes:** Re-read both new files. No dead code — every helper (`wideString`, `withWideString`, kind-mapping switch) serves multiple real call sites, not speculative. `findWidget` in `setValue` re-walks the document rather than caching `fields(of:)`'s result, matching `PDFiumAnnotationStore.findAnnotation`'s existing "re-scan, no cache" pattern (this package has no cross-call annotation/field cache anywhere; introducing one here would be a new pattern for one call site, not consistency). Security/privacy self-audit: no field *value* is ever logged (only structural things — field names/counts — could theoretically appear in a debug log, and none currently do); no network code; `setValue`'s dictionary writes (`/V`, `/AS`) go through the same `FPDFAnnot_SetStringValue` this codebase already trusts for annotation content, not a new unaudited write path.

## Handoff

**Status:** `fields(of:)` (read) is complete and real: full field tree incl. inherited-attribute resolution (PDFium's own `FPDFAnnot_GetFormField*` accessors resolve `/Parent` inheritance internally — never needed to walk the tree by hand), comb/maxlen, export values, choice options, required flag, tab order (encounter-order fallback), tested against 5 real government AcroForms (not synthetic) via tree-parity + uniqueness + the frozen conformance suite. `setValue` (write) sets the field's logical value correctly and it round-trips through this engine's own reads and through a real file save+reopen — **but does not regenerate the appearance stream or set `/NeedAppearances`**, so a value it writes is not yet verified to render correctly in Acrobat/Preview. That is this task's one open acceptance-criterion gap (NFR-C2's "values written by us render correctly in Acrobat and Preview").

**Why this is where it stopped:** the vendored PDFium build's public headers expose no direct "set value + regenerate appearance" call for text fields — real PDFium embedders either (a) simulate keystrokes via `FORM_SetFocusedAnnot`/`FORM_ReplaceSelection` (an interactive-input-simulation path, non-trivial to drive headlessly and not attempted here) or (b) set the AcroForm dictionary's `/NeedAppearances` flag so the *viewer* regenerates appearances on open — but no accessor for the AcroForm dictionary itself (as opposed to a single annotation's dictionary) was found in the vendored `fpdf_annot.h`/`fpdf_doc.h`/`fpdf_formfill.h`. This is a real engine-capability wall, not a frozen-seam issue and not a scope shortcut — flagged prominently (task file, `DocEngineHost/CLAUDE.md`, `PDFiumFormModel.swift`'s own doc comment) rather than silently claimed done.

**Exact state:** branch `task/P2-01-acroform-formmodel`, all work committed locally in this worktree (not pushed — coordinator handles push/PR from the main checkout). `git log --oneline` from the claim commit: PDFEngineAPI FormField extension (ADR-016, coordinator's commit) → `FormFillEnvironment.swift` → `PDFiumFormModel.swift` (fields/setValue) → `PDFiumFormModelTests.swift` (5 tests) → conformance-suite test addition → this Journal/CLAUDE.md doc commit (pending). `Scripts/verify.sh DocEngineHost`: OK.

**Next steps for whoever picks this up:**
1. Filed as `tasks/escalations/E-011-p2-01-formmodel-no-appearance-regeneration.md` (coordinator, post-handoff) so P2-02/P2-05 don't rediscover this gap. Investigate whether `/AcroForm`'s `/NeedAppearances` is reachable via any generic raw-dictionary accessor at the *document* level (not annotation level) in the vendored PDFium build — if `FPDF_GetXXX`-style catalog/root-dictionary access exists anywhere already used elsewhere in this codebase (grep didn't find one during this task), that's the likely path to closing the NFR-C2 gap without a PDFium version bump.
2. If no such accessor exists, this may need a PDFium version bump (its own PR, CLAUDE.md §17) to pick up whatever public API newer PDFium exposes for this, or acceptance of "value-correct, appearance-generation deferred" as this task's actual v1 scope (same class of pragmatic scoping precedent as E-009's own history) — a call for whoever owns sign-off, not something to decide unilaterally mid-task.
3. Not attempted at all: field-change notifications to consumers (Requirements' third bullet) — no observer/delegate mechanism exists on `DocumentSession`'s `FormModel` consumption path yet (that's `DocumentSession`-side wiring, a different package, out of this task's scope per the claim header).
4. `docs/specs/` gained no new file — the "expected-field manifest format" documentation the task's Documentation Updates section asks for already exists (`Fixtures/forms/README.md`, from P0-08); nothing new to write there.
