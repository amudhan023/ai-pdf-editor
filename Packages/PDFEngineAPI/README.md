# PDFEngineAPI

Engine-neutral PDF protocols and value types (render, edit, pages, annotations, forms). Protocols only - no implementations. FROZEN SEAM: changes require an ADR.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
