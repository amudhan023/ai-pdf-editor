# PolicyKit

Deterministic policy rules engine and PolicyTicket minting/verification. Pure functions over typed inputs - adding I/O here is an architecture violation.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
