# VaultStore

Vault.xpc service implementation: SQLCipher store, key hierarchy, lock state. Every privileged call requires a verified PolicyTicket. Decrypted values are SecureBytes only.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
