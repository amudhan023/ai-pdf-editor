# AuditLog

Append-only, hash-chained local audit log. Entries carry IDs/paths/hashes - the entry type has no value slot; keep it that way.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
