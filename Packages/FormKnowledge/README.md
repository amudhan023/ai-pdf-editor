# FormKnowledge

Form fingerprinting, correction-derived mapping memory, signed template packs. Stores mappings and fingerprints only - never values.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
