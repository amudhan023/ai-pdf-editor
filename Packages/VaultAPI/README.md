# VaultAPI

Vault domain model: profiles, field paths, sensitivity tiers, provenance, history lists, PolicyTicket type, client protocols + FakeVaultClient. FROZEN SEAM: changes require an ADR.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
