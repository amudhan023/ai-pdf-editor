# InferenceAPI

Typed inference request/response contracts (ocr, classify, extract, embed, generate) + FakeInferenceClient. FROZEN SEAM: changes require an ADR.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
