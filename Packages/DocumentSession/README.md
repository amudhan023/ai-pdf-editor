# DocumentSession

Document lifecycle: open/edit/atomic save/backups, undo stack, viewer + annotation + form-fill UI. Application layer.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
