# Platform

OS service wrappers: XPC transport, Keychain, LocalAuthentication, file coordination, domain event bus. Infrastructure tier.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
