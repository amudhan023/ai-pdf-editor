# IngestionSession

Ingestion workflow state machine and review UI. The only path by which extracted data reaches the vault.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
