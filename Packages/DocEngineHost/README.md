# DocEngineHost

XPC client + PDFium adapter implementing PDFEngineAPI. The ONLY package that may link the PDF engine. Runs hostile-input parsing in DocEngine.xpc.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
