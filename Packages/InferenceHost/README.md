# InferenceHost

XPC client + model registry/adapters (Vision, Core ML, FoundationModels) implementing InferenceAPI. Models load only after signature+checksum verification.

Part of Vaultform. See the repo root `CLAUDE.md` and `docs/ARCHITECTURE.md`.
