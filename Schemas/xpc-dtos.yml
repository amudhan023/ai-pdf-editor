# XPC DTO schema — single source of truth for cross-process message types.
# Codegen (Scripts/codegen.sh) is implemented by task P0-05.
# Changing this file is a frozen-seam change: ADR + human review required.
version: 1
dtos: {}   # populated by P0-05
